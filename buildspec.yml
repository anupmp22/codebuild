version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
  build:
    commands:
      - echo Build started on `date`
      - mvn test
artifacts:
  files:
    - target/gs-spring-boot-0.1.0.jar
